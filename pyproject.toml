[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lndb_setup"
authors = [{name = "Lamin Labs", email = "laminlabs@gmail.com"}]
readme = "README.md"
dynamic = ["version", "description"]
dependencies = [
    "lnschema_core>=0.15.1",
    "lamin_logger",
    "boto3",
    "cloudpathlib",
    "sqlmodel>=0.0.8",
    "appdirs",
    "supabase",
    "python-dotenv",
    "erdiagram",
    "alembic",
    "psycopg2-binary",
    "pyjwt"
]

[project.urls]
Home = "https://github.com/laminlabs/lndb-setup"

[project.optional-dependencies]
dev = [
    "pre-commit",
    "nox",
]
test = [
    "pytest>=6.0",
    "pytest-cov",
    "bionty>=0.5.0",
    "lnschema_bionty>=0.5.1",
    "lnschema_wetlab>=0.8.1",
    "lnbfx>=0.5.1",
    "cloudpathlib[gs]",
    "nbproject_test>=0.3.1",
]

[project.scripts]
lndb = "lndb_setup.__main__:main"

[tool.black]
preview = true

[tool.pytest.ini_options]
testpaths = [
    "tests",
]

[tool.coverage.run]
omit = [
    "lndb_setup/*",
]
